name: DocAI

on:
  push:
    branches: [master]

jobs:
  docai:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 2

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.12

      - name: Install Scanner
        run: pip install git+https://github.com/ManojVihari/docai.git#subdirectory=scanner

      - name: Run DocAI
        run: docai-scan --commit HEAD --server https://ae3c-14-139-82-6.ngrok-free.app
